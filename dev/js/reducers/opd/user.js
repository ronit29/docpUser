const defaultState = {
    isLoggedIn: false,
    userProfile: null,
    loaded: false,
}

export default function (state = defaultState, action) {

    switch (action.type) {

    }
    return state
}


