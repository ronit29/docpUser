const defaultState = {
    IS_LOADING : false,
    doctorsList: {}
}

export default function (state = defaultState, action) {

    switch (action.type) {
        case 'FETCH_DOCTORS': {
            
        }

    }
    return state
}