import { GET_CITIES_MAP} from '../../constants/types';

const defaultState = {
elasticData :{}
}

export default function (state = defaultState, action) {

	switch (action.type) {

		case GET_CITIES_MAP : {

		}
	}
	return state
}